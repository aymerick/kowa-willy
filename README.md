willy theme for kowa
=====================

# Install tools

    $ npm install -g grunt-cli
    $ npm install -g bower
    $ gem install bundle

# Install dependencies

    $ npm install
    $ bower install
    $ bundle install

# Build

    $ grunt build
